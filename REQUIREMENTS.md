# 需求规格说明书 (Requirements Document)

## 1. 项目概述 (Project Overview)
- **项目名称**：Forecast for Planner (供应链计划员统计与机器学习预测互动教程)
- **项目背景**：
  供应链计划员在日常工作中往往主要利用公司现有的 ERP / APS / 预测系统进行操作，大多并不了解黑盒算法背后的统计学与机器学习原理。在面对业务部门质疑、异常波动或需要手动干预（Override）时，往往缺乏科学的依据。为了让计划员更好地理解不同预测方法的底层逻辑、优缺点、适用场景以及参数对供应链库存与资金的实际影响，特开发本互动式教学与实验工具。
- **核心理念**：
  > 预测的核心不是追求虚幻的“绝对精准”，而是“降低业务不确定性”；打破“算法归算法、业务归业务”的壁垒，实现预测到库存决策的闭环。
- **核心目标**：
  开发一个网页版/Jupyter Notebook 互动式工具，通过“**业务痛点引导 + 交互控件调参（滑杆/开关） + 实时可视化反馈 + 业务启发贴士**”，让计划员直观掌握从数据清洗、传统统计、现代机器学习到库存决策的全链路知识。

---

## 2. 目标用户与角色 (Target Users & Roles)
- **主要角色**：
  - **初/中级计划员 (Planner)**：学习并理解各种时间序列的构成成分、传统统计预测方法与现代机器学习方法，提升日常调参和异常归因能力。
  - **S&OP / 需求与供应链分析师**：用于复杂市场情景推演（促销、降价、外部节假日冲击）。
  - **供应链管理者 / 培训导师**：作为团队内部培训、业务赋能与沟通的互动式沙盘教具。

---

## 3. 功能需求详细规范 (Functional Requirements)

> **需求编写规范说明**：每个功能模块均按照【需求编号/名称】、【业务痛点】、【核心原理与公式】、【交互控件设计 (UI/Widget)】、【图表可视化输出】、【计划员业务启发 (Tooltips)】6 个统一维度进行规范化定义。

---

### 3.0 前言与数据准备：建立核心观念

#### 3.0.1 模块 0：数据清洗与异常处理交互实验 (Data Cleaning Playground)
* **需求编号**：REQ-MOD-00
* **模块名称**：时序数据清洗与异常值修复实验
* **业务痛点**：
  * 历史销售数据中常常混杂“大宗偶发爆单（大客户一次性团购）”或“虚假零销量（因工厂缺料断供导致实际销量为 0）”。直接喂给预测模型会导致严重失真：把断货当成需求萎缩而减少备货（恶性断货），或把单次爆单当成爆发增长而盲目备货（呆滞积压）。
* **核心原理**：
  * **异常值识别**：$3\sigma$ 原则或 IQR（四分位距法，$\text{上限} = Q3 + k \times IQR$）。
  * **异常值修复策略**：Winsorization（盖帽截断法）、中位数/均值替换、直接剔除。
  * **缺失值/缺货归零修复策略**：线性插值 (Linear Interpolation)、历史同期填充 (Year-over-Year)、前后向填充 (FFill/BFill)。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **异常值处理策略** | 单选按钮 (Radio) | `保留原始脏数据` / `IQR 盖帽截断 (Winsorize)` / `中位数替换` / `剔除并线性插值` | 保留原始脏数据 | 应对大宗偶发爆单的清洗方法 |
  | **异常灵敏度阈值 $k$** | 滑杆 (Slider) | `1.0` 至 `3.0`，步长 `0.2` | `1.5` | 判定为异常点的严格程度 |
  | **缺货断点处理策略** | 单选按钮 (Radio) | `保留为 0 (忽视缺货)` / `线性插值补齐` / `历史同期填充` | 保留为 0 | 应对缺货导致销量为 0 的修正策略 |
* **图表可视化输出**：
  1. **数据对比图**：原历史线（红色高亮标出异常爆单点与灰色缺货归零区间） vs 清洗修正后的平滑曲线（绿色实线）。
  2. **清洗前后统计指标卡片**：即时显示清洗前后的均值、标准差与变异系数 ($CV$) 对比。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “垃圾进，垃圾出 (Garbage In, Garbage Out)！分清‘因缺货导致的零销量’与‘市场没有需求的真实零销量’是计划员的基本功。如果不加修正就把缺货当作无需求，系统在下一周期会进一步减少计划量，导致‘恶性断货循环’。”

---

### 3.1 第一阶段：传统统计方法（建立业务基准线 Baseline）

#### 3.1.1 模块 1：数据拆解 (Time Series Decomposition)
* **需求编号**：REQ-MOD-01
* **模块名称**：时间序列三要素拆解与即时合成
* **业务痛点**：
  * 销量大涨时，分不清是周期性旺季爆单，还是产品生命周期处于高增长期，亦或是不可控的偶然扰动（噪音），导致错误增加备货。
* **核心原理**：
  * 加法模型：$Y_t = Trend_t + Seasonal_t + Residual_t$（季节波动绝对幅度保持恒定）。
  * 乘法模型：$Y_t = Trend_t \times Seasonal_t \times Residual_t$（季节波动幅度随基本盘增大成比例放大）。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **分解模式** | 单选按钮 (Radio) | `加法模型 (Additive)` / `乘法模型 (Multiplicative)` | 加法模型 | 选择序列各成分的合成机制 |
  | **趋势斜率 (Trend)** | 滑杆 (Slider) | `-5.0` 至 `+10.0`，步长 `0.5` | `2.0` | 代表销量长期的基本盘增长/萎缩速度 |
  | **季节性振幅 (Seasonality)**| 滑杆 (Slider) | `0` 至 `50`，步长 `1` | `20` | 代表淡旺季差异的剧烈程度（周期固定为 12 个月） |
  | **随机噪音强度 (Noise)** | 滑杆 (Slider) | `0` 至 `30`，步长 `1` | `5` | 代表市场偶发不可控扰动的方差大小 |
* **图表可视化输出**：
  1. **主图**：展示合成后的最终销售历史曲线 $Y_t$。
  2. **分量子图**：上下并列展示 3 条子曲线：
     - 子图 1：`Trend` 纯趋势线（看清大方向）；
     - 子图 2：`Seasonal` 纯季节波形（看清规律性节奏）；
     - 子图 3：`Residual` 白噪音波动（看清随机杂讯）。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “试着把 Noise（噪音）拉到最大，你会发现原本清晰的季节波峰变得模糊难辨——在实际业务中，过滤噪音、识别真实趋势是避免‘牛鞭效应’的第一步。”

#### 3.1.2 模块 2：移动平均 (Moving Average)
* **需求编号**：REQ-MOD-02
* **模块名称**：移动平均平滑与反应滞后体验
* **业务痛点**：
  * 销售经理建议看“过去 3 个月平均”，采购经理建议看“过去 12 个月平均”，计划员在平滑噪声与快速响应市场拐点之间难以权衡。
* **核心原理**：
  * 简单移动平均 (SMA)：$\hat{Y}_{t+1} = \frac{1}{k} \sum_{i=0}^{k-1} Y_{t-i}$
  * 加权移动平均 (WMA)：$\hat{Y}_{t+1} = \sum_{i=0}^{k-1} w_i Y_{t-i}$，其中近期赋更高权重。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **平滑方法** | 下拉选框 (Dropdown) | `简单移动平均 (SMA)` / `线性加权移动平均 (WMA)` | SMA | 权重分配机制 |
  | **滑动窗口 (Window Size $k$)**| 滑杆 (Slider) | `2` 至 `24` 个月，步长 `1` | `3` | 计算平均所参考的历史期数 |
* **图表可视化输出**：
  * 同一张图叠加绘制：
    1. **灰色折线**：实际历史销量（预设含剧烈拐点的测试序列）。
    2. **蓝色粗线**：移动平均预测线。
  * **动态滞后标记 (Lag Indicator)**：在曲线发生拐点的位置，用虚线箭头标出预测线相比实际拐点滞后了多少期。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “滑动窗口 $k$ 越长，曲线越平稳（不受偶发单笔采购干扰），但对市场拐点的反应也越迟钝（滞后严重）。当产品进入生命周期衰退期时，大窗口会导致严重的高估预测与库存积压！”

#### 3.1.3 模块 3：指数平滑 (Holt-Winters Exponential Smoothing)
* **需求编号**：REQ-MOD-05
* **模块名称**：三次指数平滑与多参数敏感度调控
* **业务痛点**：
  * 移动平均对历史数据是一刀切或线性赋权，而指数平滑能够赋予近期数据指数级递减的记忆。但 ERP 系统中的 $\alpha, \beta, \gamma$ 参数犹如天书，计划员不知道参数调大调小对业务预测意味着什么。
* **核心原理**：
  * 水平项 (Level): $L_t = \alpha (Y_t - S_{t-m}) + (1-\alpha)(L_{t-1} + T_{t-1})$
  * 趋势项 (Trend): $T_t = \beta (L_t - L_{t-1}) + (1-\beta) T_{t-1}$
  * 季节项 (Seasonality): $S_t = \gamma (Y_t - L_{t-1} - T_{t-1}) + (1-\gamma) S_{t-m}$
  * 阻尼趋势因子 $\phi$ (Damped Factor): 防止长期线性趋势无限上翘外推。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **水平权重 $\alpha$ (Alpha)** | 滑杆 (Slider) | `0.01` 至 `0.99`，步长 `0.05` | `0.30` | 近期基准水平敏感度（大=追随最新变动，小=依赖历史平稳） |
  | **趋势权重 $\beta$ (Beta)** | 滑杆 (Slider) | `0.01` 至 `0.99`，步长 `0.05` | `0.10` | 斜率趋势更新敏感度 |
  | **季节权重 $\gamma$ (Gamma)** | 滑杆 (Slider) | `0.01` 至 `0.99`，步长 `0.05` | `0.20` | 季节周期波形更新灵敏度 |
  | **阻尼系数 $\phi$ (Phi)** | 滑杆 (Slider) | `0.80` 至 `1.00`，步长 `0.02` | `0.98` | 抑制趋势过度盲目外推的收敛因子 |
* **图表可视化输出**：
  1. **走势与未来外推图**：历史拟合线 + 未来 12 期滚动预测延伸线，带 95% 置信区间。
  2. **模型风格仪表卡**：根据参数组合实时评估并贴标当前状态（如：“敏捷激进型 / 追涨杀跌” 或 “沉稳抗噪型 / 钝化保守”）。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “$\alpha$ 调得过高（如 >0.8），模型会像‘惊弓之鸟’，把客户的一次偶发加单当成大势已来；$\alpha$ 过低（如 <0.1），模型则会变成‘迟钝的恐龙’。阻尼因子 $\phi$ 则是防止新产品上线初期预测‘盲目上天’的刹车器。”

---

### 3.2 第二阶段：机器学习与现代模型（应对复杂市场动态）

#### 3.2.1 模块 4：促销与节假日特征工程 (Feature-based ML: XGBoost / Random Forest)
* **需求编号**：REQ-MOD-03
* **模块名称**：引入促销与折扣特征的监督学习预测
* **业务痛点**：
  * 下个月有“双 11”大型促销，折扣打 6 折。传统时序方法只看历史销量的惯性，完全无法捕捉促销带来的爆发式增量，造成断货。
* **核心原理**：
  * 将时间序列转换为监督学习特征矩阵：$[Lag_1, Lag_2, \text{Promo\_Flag}, \text{Discount\_Rate}, \text{Holiday\_Flag}] \rightarrow Y$
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **未来促销开关** | 复选框 (Checkbox) | `开启 (True)` / `关闭 (False)` | 关闭 | 未来指定周期是否举办促销活动 |
  | **折扣力度 (Discount)** | 滑杆 (Slider) | `5 折 (0.5)` 至 `95 折 (0.95)`，步长 `0.05` | `8 折 (0.8)` | 降价让利幅度 |
  | **特征重要性开关** | 开关 (Toggle) | `显示 / 隐藏` | 显示 | 是否查看算法做出决策的依据 |
* **图表可视化输出**：
  1. **趋势对比图**：传统平滑模型（平缓延伸） vs 机器学习模型（在促销期拉出爆发尖峰）对比。
  2. **促销增量与透支分析 (Lift & Dip Effect)**：高亮标出促销期销量拉升增幅 (Lift)，以及促销结束后紧随而来的“需求透支低谷 (Dip)”。
  3. **特征贡献柱状图 (Feature Importance)**：直观展现 `Discount_Rate`、`Lag_1`、`Month` 各自的决策权重。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “传统时序模型适合预测‘自然需求’，而机器学习能通过外部特征捕捉‘人造爆发’。但计划员一定要警惕：大促销通常会透支消费者后续 1~2 个月的需求，促销期过后一定要下调备货量！”

#### 3.2.2 模块 5：工业级快速部署利器 (Facebook Prophet)
* **需求编号**：REQ-MOD-06
* **模块名称**：Prophet 变点检测与节假日分解
* **业务痛点**：
  * 公司 SKU 成千上万，没有算法工程师逐个调参。此外，法定节假日（如春节、国庆、中秋）每年公历日期变动，传统模型无法对齐日历脉冲。
* **核心原理**：
  * 广义加性模型 (GAM)：$y(t) = g(t) + s(t) + h(t) + \epsilon_t$
  * $g(t)$ 趋势项（包含自动检测的分段变点 Changepoints）。
  * $s(t)$ 周期项（周度、月度、年度季节性）。
  * $h(t)$ 节假日脉冲效应项。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **节假日效应开关** | 开关 (Toggle) | `开启 (True)` / `关闭 (False)` | 开启 | 是否自动加载法定节假日字典 |
  | **变点灵敏度 (Changepoint Scale)** | 滑杆 (Slider) | `0.001` 至 `0.5`（对数步进） | `0.05` | 趋势转折点捕捉灵活性（小=平滑欠拟合，大=灵活过拟合） |
  | **季节性模式** | 单选按钮 (Radio) | `加法 (Additive)` / `乘法 (Multiplicative)` | 加法 | 节假日与季节波形叠加机制 |
* **图表可视化输出**：
  1. **主预测图**：显示自动识别出的历史拐点（虚线标出 Changepoints）及带置信区间的未来预测。
  2. **组件拆解看板 (Components Plot)**：
     - 子图 1：长期大势走向；
     - 子图 2：周度效应（周一至周日哪天销量最高）；
     - 子图 3：年度效应（几月份是自然淡季/旺季）；
     - 子图 4：节假日脉冲图（放假前几天的集中囤货效应）。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “Prophet 的最大优势在于‘业务透明度’。利用其组件拆解图，你可以向销售和生产团队直观证明：‘春节前 2 周客户会集中突击下单 35%，节后首周需求骤降 60%’，从而合理安排工厂放假与节前备库。”

---

### 3.3 第三阶段：业务闭环（从预测数字落地到库存决策）

#### 3.3.1 模块 6：预测误差体系与偏向性看板 (Error Metrics & Forecast Bias)
* **需求编号**：REQ-MOD-07
* **模块名称**：预测误差多指标计算与系统性偏向预警
* **业务痛点**：
  * 汇报时常说“预测准确率 85%”，但仓库依然面临“畅销款缺货断供、滞销款积压爆仓”。单纯看单一准确率指标掩盖了预测究竟是“系统性偏高”还是“系统性偏低”。
* **核心原理**：
  * $\text{MAE} = \frac{1}{n} \sum |A_t - F_t|$（绝对误差：平均每期偏离多少件实物）
  * $\text{MAPE} = \frac{1}{n} \sum |\frac{A_t - F_t}{A_t}| \times 100\%$（相对误差：百分比形式，但低销量时会发生除零暴增失真）
  * $\text{WAPE} = \frac{\sum |A_t - F_t|}{\sum A_t} \times 100\%$（加权百分比误差：消除除零问题，反映大盘真实偏差）
  * $\text{Forecast Bias} = \frac{\sum (F_t - A_t)}{\sum A_t} \times 100\%$（预测偏向性）：
    - $\text{Bias} > 0$：乐观过剩偏向 $\rightarrow$ 造成呆滞库存、仓储爆仓与资金沉淀；
    - $\text{Bias} < 0$：悲观短缺偏向 $\rightarrow$ 造成频繁断货、丢单与高额紧急调货运费。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **人工干预偏移量 (Bias Shift)** | 滑杆 (Slider) | `-50%` 至 `+50%`，步长 `5%` | `0%` | 模拟计划员主观将预测整体调高或调低 |
  | **间歇性低销量场景注入** | 开关 (Toggle) | `开启` / `关闭` | 关闭 | 注入连续偶发低销量（观察 MAPE 失真现象） |
* **图表可视化输出**：
  1. **走势与残差阴影图**：实际销售线与预测线之间的红（高估）绿（低估）偏差阴影。
  2. **四合一指标卡片**：MAE、MAPE、WAPE、Bias 同台对照。
  3. **业务风险预警雷达 (Risk Alert)**：
     - 当 $\text{Bias} > +10\%$：亮起红色“库存积压与报废风险”警报；
     - 当 $\text{Bias} < -10\%$：亮起黄色“缺料停产与客户流失”警报。
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “永远不要只看单点准确率！一个平均准确率 90% 但 Bias 为 +25% 的预测方案，在供应链上是吞噬企业现金流的‘隐形杀手’。控制好 Bias（方向）往往比扣最后 1% 的精度对公司更为致命！”

#### 3.3.2 模块 7：预测误差与安全库存博弈 (Safety Stock & Service Level)
* **需求编号**：REQ-MOD-04
* **模块名称**：服务水平与库存持有成本的指数博弈计算器
* **业务痛点**：
  * 销售总监要求将交付订单满足率（Service Level）从 95% 提高到 99.5%，认为只是“多备一点点货”，计划员需要用数据证明背后的财务资金代价。
* **核心原理**：
  * 预测误差：$\sigma_D$ 为预测残差的标准差；
  * 安全库存：$SS = Z \times \sigma_D \times \sqrt{L}$，其中 $Z = \Phi^{-1}(\text{Service Level})$；
  * 年持有成本：$\text{Holding Cost} = SS \times \text{Unit Cost} \times \text{Holding Rate}$。
* **交互控件设计 (UI/Widget)**：
  | 控件名称 | 控件类型 | 参数范围 / 选项 | 默认值 | 业务含义 |
  | :--- | :--- | :--- | :--- | :--- |
  | **服务水平 (Service Level)** | 滑杆 (Slider) | `90.0%` 至 `99.9%`，步长 `0.1%` | `95.0%` | 期望的订单满足率（不缺货概率） |
  | **采购提前期 (Lead Time $L$)** | 滑杆 (Slider) | `1` 至 `12` 周，步长 `1` | `4` 周 | 供应链交期长度 |
  | **单件成本 (Unit Cost)** | 数字输入框 | `10` 至 `1000` 元 | `100` 元 | SKU 单件采购成本 |
  | **年持有成本率** | 滑杆 (Slider) | `10%` 至 `40%` | `20%` | 包括仓储费、资金占用利息、呆滞报废损失率 |
* **图表可视化输出**：
  1. **库存动态水位图**：显示“预测基本需求线”之上的“安全库存警戒线 (Safety Stock Line)”。
  2. **非线性成本爆炸曲线**：横轴为服务水平（90% ~ 99.9%），纵轴为持有成本。随着滑杆拉向 99.9%，曲线呈现急剧向上的“陡峭爬坡”，并实时计算显示：“服务水平从 95% 提升到 99%，库存资金增加 XX 万元！”
* **计划员业务启发 (Tooltips)**：
  > 💡 **计划员小贴士**：
  > “当服务水平越接近 100%，$Z$ 值增长趋于无穷大！追求最后的 1% 不缺货，需要付出成倍的库存持有成本。计划员的价值就是找到最佳平衡点，而不是无底线堆库存。”

---

## 4. 非功能性需求与交互体验 (Non-Functional & UX)
- **4.1 响应实时性**：滑杆拖拽与控件切换时，计算与图表重绘延迟保持在 **< 200ms**，保证如同桌面端软件般的即时交互体验。
- **4.2 零环境负担**：支持免安装开箱即用体验（如基于轻量 Web 前端、Streamlit 或单文件一键式 Jupyter Notebook）。
- **4.3 术语规范性**：全篇采用严谨、规范的供应链行业中英文对照术语（如 Safety Stock、Lead Time、Lift Effect、Forecast Bias）。

---

## 5. 里程碑交付计划 (Milestones & Roadmap)

| 阶段 | 交付模块 | 核心验收标准 |
| :--- | :--- | :--- |
| **Phase 1: 基础框架与传统统计** | REQ-MOD-00 (数据清洗)<br>REQ-MOD-01 (时序拆解)<br>REQ-MOD-02 (移动平均)<br>REQ-MOD-05 (Holt-Winters) | 完整交互控件可用，可动态调参并实时观察趋势、平滑、滞后与周期外推 |
| **Phase 2: 现代特征与机器学习** | REQ-MOD-03 (促销 XGBoost)<br>REQ-MOD-06 (Prophet 变点) | 实现时序监督学习转换，支持促销开关拉升与日历效应组件拆解 |
| **Phase 3: 业务闭环与综合看板** | REQ-MOD-07 (误差看板)<br>REQ-MOD-04 (安全库存博弈) | 完整计算 MAE/MAPE/Bias，并实现服务水平到库存资金成本的非线性爆炸曲线展示 |
