# 实现偏差记录 (Decisions & Deviations)

本文档记录**实际实现与 IMPLEMENTATION.md 不一致的地方、以及为什么**。
原则：凡是与文档不符之处，要么在这里写清楚理由与实测证据，要么就不算完成。

---

## D1. 构建方式：零构建 + vendor ESM，替代 Vite + Vue 3 + GitHub Actions

| | 文档方案 | 实际方案 |
| :--- | :--- | :--- |
| 应用代码 | Vite 打包 Vue 3 SFC | 原生 ES Module，**无构建** |
| 第三方库 | npm 依赖（隐含走 CDN/打包） | `vendor/echarts.esm.js` 提交进仓库 |
| 部署 | GitHub Actions 编译后发布 | 直接推送静态文件 |

**理由**
1. **CDN 在国内不可靠**：本机实测 `cdn.jsdelivr.net` 直接 0 字节超时。依赖必须进仓库。
2. **体积反而更小**：用 esbuild 只注册本项目用到的 echarts 子集（line/bar/scatter +
   grid/tooltip/legend/title/markLine/markPoint/markArea/dataZoom/toolbox/visualMap/
   graphic + canvas 渲染器），产物 **638 KB**；全量 `echarts.min.js` 是 **1007 KB**，省 37%。
3. **自己的代码零构建 → 整个算法层可被 `node --test` 覆盖**。本项目 8 个算法模块
   全是纯函数、无 DOM 依赖，113 项单测直接跑在 node 上，不需要 jsdom、不需要浏览器。
   这是本项目发现真 bug 的主要手段（见 D9）。
4. **部署环节更少**：Pages 只需要静态文件，不需要 runner、`npm ci`、Node 版本对齐。
5. **可回退**：`src/core/` 完全不受影响。若要改回 Vite，只需加 `vite.config.js`
   并把 `src/modules/*.js` 交给打包器 —— core 一行都不用动。

**代价（如实记录）**：没有 HMR，没有 Vue 响应式。8 个模块靠统一的状态对象 + `render()`。
UI 层因此必须遵守 `references/vanilla-js-rerender-lifecycle.md` 里那套 innerHTML
重渲染纪律（状态保留、图表销毁、懒绘制、控件必须真的接线）。

---

## D2. 促销与 Prophet 模块：改成**真拟合**，不是"仿真"

IMPLEMENTATION.md 把它们描述为"促销脉冲与透支效应特征模型""周期傅里叶级数与节假日加性分解"，
读起来像手工构造的示意曲线。实际实现是真做统计推断：

- `linalg.js` 提供内部标准化的 OLS，返回**系数、标准误、t 值、R²/调整 R²、残差 σ**；
  解完通过雅可比矩阵把系数**与标准误一起**换算回原尺度。
- `promo_sim.js` 用 `[Lag1, Lag2, Promo标志, (1−d)×Lag1 交互项, 月正弦/余弦, 节假日]`
  构造监督学习矩阵，然后**从数据里把折扣弹性 κ 学回来**。
- `prophet_sim.js` 用 `{1, t, max(0,t−cp)}` 分段线性 + 周度傅里叶 + 年度傅里叶 +
  节假日窗口指示向量做 GAM 拟合，每张组件小图都是可归因的。

**为什么值得多花这些功夫**：生成侧的数据集带 ground truth（真实 κ、真实斜率、真实节假日效应），
于是"模型学回来对不对"从一个视觉印象变成了**可断言的数字**。测试里既有正向断言，
也有"把变点位置给错时应该学不回来"的负向对照，证明断言有区分度。

**衍生收益**：`linalg.js` 的秩亏护栏当众暴露了一个真问题 —— 历史里只有一次促销时，
促销标志与强度交互列近乎恒定，设计矩阵接近秩亏（实测最小/最大主元 ≈ 2.7e-18），
模型**直接拒绝求解**。这比返回一个看起来确定的错数字好得多，也正好是计划员该学的：
数据里没有的信息，再强的模型也变不出来。

---

## D3. 数据清洗顺序：先修缺货，再判异常（文档表格顺序相反）

IMPLEMENTATION.md §3.0.1 的控件表先列"异常值处理策略"再列"缺货断点处理策略"。
但执行顺序必须是**先修缺货、再判异常**：

- 缺货期被置 0，会把 Q1 拉低，下界掉到 0 附近 → 真正的低异常点反而漏检；
- 反过来把 0 当异常处理，会被盖帽抬到上界之上，丢掉"缺货"这个语义。

因此实现为：
- 缺货策略 ≠ `保留为 0` → 先补齐零值，再在补齐后的序列上做异常检测；
- 缺货策略 = `保留为 0` → 零值**豁免**：不参与 IQR 统计、也不被当异常值盖帽
  （它们已由计划员判定为缺货），并在 `meta.exemptZeroCount` 里回报给 UI。

UI 上会在模块 0 明确标注这个执行顺序。

---

## D4. Holt-Winters：明确声明"不能复现 statsmodels"

本实现是「固定初始状态（前两个完整季节）+ 用户给定 α/β/γ/φ 递推」。
statsmodels 的 `ExponentialSmoothing` 会**联合最优化初始状态与平滑参数**，
因此本实现**不可能逐位复现**它的拟合值。可以复现的只有：

- 显式给定参数时的递推公式本身（逐位精确，测试用手算极小样本钉住）；
- 同参数下的量级一致（RMSE 同阶）。

任何"与 Python 完全一致"的说法对这个模块都不成立，测试注释里也写明了。

---

## D5. Φ⁻¹ 的精度：写成"实测偏差"而不是"精确计算"

IMPLEMENTATION.md 说"高精度…精确计算"。实测（与 `scipy.stats.norm.ppf` 对照）：

```
与 scipy 偏差：最大 9.3e-15（p=0.999），典型 4.4e-16
回环 Φ(Φ⁻¹(p)) 与 p 的偏差：最大 2.8e-17（不到 1 ULP）
```

偏差来自 erfc 的 Chebyshev 逼近自身（实测 `erfc(0) = 1.0000000000000016`，绝对误差 1.6e-15）。
对安全库存而言这远超需求（σ 本身就是估出来的），但文档里不会宣称能复现 scipy 末位。

---

## D6. 安全库存的"成本爆炸"：只声称**可证明**的部分

需求文档的小贴士说"服务水平越接近 100%，Z 值增长趋于无穷大"。数学上成立，
但在 99.9% 处**并没有**爆炸：Z(95%)=1.645 → Z(99%)=2.326 → Z(99.9%)=3.090，
相对增幅是 +41% 然后 +33%（在减小）。

可证明的是**凸性**：σ=100、L=4、单价 100、持有率 20% 时，
成本曲线斜率从 95%→99% 段的约 6.8 万/单位服务水平，跳到 99%→99.9% 段的约 34 万
——**放大约 5 倍**。测试断言的是这个（斜率比 > 3），UI 文案也据此措辞。

---

## D7. 模块 5（Prophet）改用**日粒度**数据

IMPLEMENTATION.md 要求"周度效应（周一至周日哪天销量最高）"子图，但给了月度数据的语境。
月粒度上根本不存在周度季节性。因此模块 5 用 3 年日数据（1095 天）：

- 周度季节性、年度季节性可以同时拆出来；
- "春节前两周集中突击下单"这种窗口效应能看出**形状**（月粒度会把窗口糊成一格）；
- 变点检测能定位到"哪一天拐的"。

日期一律用 UTC 整数天（`epochDay`）运算，不做任何本地时区换算 ——
本地时区 + 夏令时会让日期在边界上偏移一天，这类 bug 极难发现。

---

## D8. 促销数据集设计：经过**实测标定**，不是随手编的

折扣弹性 κ 只有在"折扣力度有足够变化"时才可识别。实测（8 个种子，`scripts/calibrate-promo.mjs`）：

| 数据集设计 | κ 还原平均相对误差 | 最差 |
| :--- | :--- | :--- |
| 4 次促销、折扣雷同 | 20.5% | 41.0% |
| 6 次促销、区间翻倍 | 28.5% | 45.6% |
| **8 次促销、折扣 5 折~95 折** | **6.7%** | **16.2%** ← 采用 |
| 10 次促销 | 9.5% | 18.0% |

取舍逻辑：次数太少信息不足；次数太多则透支期与下一次促销互相污染。测试容差按上表标定，
不是拍脑袋写的阈值。

---

## D9. 开发过程中被测试抓出的两个真 bug（保留为回归测试）

1. **哨兵值被当成真值**：`peakLag` 把平滑序列的 `null` 换成 `-Infinity` 再交给
   `findPeaks`，被输入校验直接拒绝。修为跳过非有限值。
2. **样本短于变点位置 → 整个模块崩掉**：数据只有 400 天而变点设在 `[365, 730]` 时，
   第二列 `max(0, t−730)` 恒为 0，与截距完全共线，底层 OLS 拒绝求解。
   同类问题还有"数据没覆盖到某个节假日"（把范围截到年中，国庆窗口两列全是 0）。
   修法是在 `buildGamDesign` 里加**通用护栏**：剔除全样本恒定的列并回报列名。
   两个场景都补了回归测试。

这类 bug 在浏览器里表现为"图表空白"或"模块报错"，极难定位；在纯函数层被抓住是零成本的。

---

## D10. 由真浏览器探针与截图抓出的第二批缺陷

**这一批单元测试完全无感** —— 它们全都是"渲染出来才看得见"的问题。
记录在此，同时也是"为什么截图与真浏览器检查不可省略"的证据。

### 渲染链

1. **图表在容器脱离 document 时初始化**（最严重）。模块 `create()` 里调用 `setOption` 时，
   容器还没挂进文档，ECharts 量到的宽度是 0（高度因为有内联样式反而是对的）；
   zrender 在尺寸为 0 时**根本不会创建 canvas**，之后 `resize()` 也救不回来。
   症状：页面结构齐全、图表区域一片空白，且只在切换模块时出现。
   修法：把首绘移到挂载之后（`app.js` 的 `activate()` 负责调 `api.update()`），
   并在 `getChart()` 里加"容器未挂载"告警。
2. **多子图轴缺 `gridIndex`**。ECharts 的轴默认 `gridIndex = 0`，**不会**按数组下标自动分配。
   漏掉之后 4 个子图的坐标轴全部画在第一个网格上，渲染成"标签糊成一团 + 下面大片空白"。
   修法：`catAxis`/`valAxis` 支持 `gridIndex`，多子图逐个指定；网格位置改用像素值。
   探针补了结构性断言：轴的 gridIndex 必须互不相同、网格 tops 必须互不相同。
3. **`markPoint.data` 写成裸数组** → ECharts 把内层数组的元素当数据项写 `.label` 属性，
   抛 `Cannot create property 'label' on number ...`，整个模块崩掉。必须用 `{coord: [x, y]}`。
4. **负数堆叠画误差阴影**不可靠：区域会锚到 0 轴以下（截图里阴影跑到图表底部）。
   改用"锚在较低那条线的正向堆叠"，区间两端必然落在两条线之间。
5. **Y 轴名与图例重叠**：轴名画在网格左上角，图例默认放左边就会压住它。
   修法：图例统一靠右（`baseOption` 一处改，所有图受益）。
6. **控件列 320px 太窄**导致 8 字标签折行成两行。加宽到 360px。

### 文案渲染

7. **`textContent` 渲染带标签的文案**：模块副标题（`moduleHeader`）与贴士（`tipBox`）
   把 `<strong>`、`<br>`、`&gt;` 当字面文字显示在页面上。DOM 断言完全查不到
   （文字只是"变长了"）。修法：这两处改为按 HTML 渲染；
   探针新增文本模式扫描（未解析标签 / 未转义实体），把这类缺陷变成可自动检测的。

### 工具链

8. **`browser-check` / `screenshot` 用固定 profile 目录** → Chrome 恢复上次的标签页，
   `/json/list` 抓到陈旧页面，于是探针在一个跑着旧代码的页面上给结论，
   而且 ready gate 会以 1ms 的**假速度**通过。修法：每次运行使用唯一 profile 目录。
   这个坑会污染整轮验证结论，比它看起来严重。
9. **探针指纹不够灵敏**：早期只取每个序列末尾 3 个点 + KPI 主值，
   于是"清洗策略只改了序列中段的爆单点"这类真实生效的控件被误判为死控件。
   修法：改为全序列数值校验和 + 同时读 KPI 主值与副文案 + 表格行数 + 渲染高度。
   教训：**一个从没失败过的探针不是证据**。

### 产品可用性

10. **mod0 的 k 滑杆在 1.5~3.0 全范围无反馈**：数据集只有 3.2 倍的极端爆单，
    远超任何 k 的上界，拖动滑杆什么都不会变 —— 计划员会以为工具坏了。
    修法：数据侧补 4 处**中等幅度异常**（1.2~1.7 倍，落在判定边界之间），
    并加一条可用性断言：截断数必须随 k 单调不增**且确有变化**。
    这是"数据设计也是产品设计"的一个具体例子。

---

## D11. 尚未完成（与文档的差距，不要当成已完成）

- `notebooks/*.ipynb` 配套 Python 课件 —— 未实现。
- `public/retail_sales_sample.csv` 等样例数据文件 —— 未添加（当前全部由生成器产出，这是有意的：
  生成数据带 ground truth，"模型能否学回真值"才成为可断言的命题）。
- 用户自带数据上传（CSV/XLSX）—— 需求文档未要求，暂未实现。
- GitHub Actions 流水线 —— 按 D1 不需要；`.github/workflows/` 目录保留但为空。
- `README.md` —— **已完成**（含四层验证命令与诚实边界清单）。
